<Window x:Class="BinaryTreeProject.Views.BinaryTree"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:BinaryTreeProject.ViewModels"
        xmlns:local="clr-namespace:BinaryTreeProject.Views"
        mc:Ignorable="d"
        Title="BinaryTree" MinHeight="450" MinWidth="800">
    <Window.Resources>
        <vm:BinaryTreeViewModel x:Key="vm" />
    </Window.Resources>
    <Grid DataContext="{StaticResource vm}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Vertical" Grid.Row="0" >
            <ToolBar ToolBarTray.IsLocked="True">
                <Button Width="60" Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/save.png" Width="30" />
                            <TextBlock Text="Sačuvaj" Margin="0 5 0 0" FontWeight="Bold" />
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Width="60"  Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/load.png" Width="30" />
                            <TextBlock Text="Učitaj" Margin="0 5 0 0" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Width="60"  Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/undo.png" Width="30" />
                            <TextBlock Text="Undo" Margin="0 5 0 0" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Width="60"  Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/redo.png" Width="30" />
                            <TextBlock Text="Redo" Margin="0 5 0 0" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Width="60"  Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/add.png" Width="30" />
                            <TextBlock Text="Dodaj" Margin="0 5 0 0" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <Button Width="60"  Background="Transparent" BorderThickness="0" Padding="5">
                    <Button.Content>
                        <StackPanel Orientation="Vertical">
                            <Image Source="/Icons/delete.png" Width="30" />
                            <TextBlock Text="Izbriši" Margin="0 5 0 0" FontWeight="Bold"/>
                        </StackPanel>
                    </Button.Content>
                </Button>

                <TextBox Text="{Binding NewNodeValue, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Width="100" />
                <Button Command="{Binding AddNewNodeCommand}" CommandParameter="{Binding NewNodeValue}">Dodaj</Button>
            </ToolBar>

            <Border BorderBrush="Black" BorderThickness="1"/>
        </StackPanel>

        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" CanContentScroll="True" Grid.Row="1">
            <Canvas Width="3000" Height="2000" x:Name="MainCanvas">
                <ItemsControl ItemsSource="{Binding BinaryTree.Nodes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemContainerStyle>
                        <Style TargetType="ContentPresenter">
                            <Setter Property="Canvas.Left" Value="{Binding Position.X}" />
                            <Setter Property="Canvas.Top" Value="{Binding Position.Y}" />
                        </Style>
                    </ItemsControl.ItemContainerStyle>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Ellipse DataContext="{StaticResource vm}" Width="{Binding BinaryTree.CircleDiameter}" Height="{Binding BinaryTree.CircleDiameter}" Stroke="Black" StrokeThickness="3" />
                                <TextBlock FontWeight="Bold" FontSize="20" Text="{Binding Value}" HorizontalAlignment="Center" VerticalAlignment="Center" />
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl ItemsSource="{Binding BinaryTree.LinePositions}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Line Stroke="Black" StrokeThickness="3"
                                  X1="{Binding StartPosition.X}" Y1="{Binding StartPosition.Y}"
                                  X2="{Binding EndPosition.X}" Y2="{Binding EndPosition.Y}"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </Canvas>
        </ScrollViewer>
    </Grid>
</Window>
